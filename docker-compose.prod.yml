version: '3'

services:
  app:
    image: 'ivaantorres/real-time-chat-app:0.1'
    ports:
      - '80:80'
    container_name: app

  mongo:
    image: 'mongo:5.0.6'
    ports:
      - '27017:27017'
    container_name: mongo
    volumes:
      - '/c/data/db:/data/db'
